# Unique filename avoids KAG's basename collision with CTF/mapcycle.cfg.
# The real Gloryhill regression map is copied to Maps/AIBTest/8x_Gloryhill.png.
# The automated suite still uses the large kaggen arena because most scenarios
# reshape it with server_SetTile.

mapcycle = Maps/AIBTest/aib_flat_harvest.kaggen.cfg;
